apiVersion: v1
kind: ConfigMap
metadata:
  name: greet-values
data:
  GREETING: "Hello from pod"
  APP_VERSION: "1.0.0"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-conf
data:
  default.conf: |
    server {
      listen 80;
      location / {
        proxy_pass http://127.0.0.1:8000;
      }
    }